import 'nav.dart';

List<FFRoute> routes = [
  // FFRoute(
  //   name: '_initialize',
  //   path: '/',
  //   builder: (context, _) =>
  //       appStateNotifier.loggedIn ? const NavBarPage() : const LoginPageWidget(),
  // ),
];
